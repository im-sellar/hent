> Journal de bord complet de l'exécution du plan, des neuf tâches à la revue
> finale de branche. L'original vivait dans `.superpowers/sdd/`, hors dépôt et
> hors sauvegarde ; cette copie est le seul exemplaire qui subsiste.

# SDD ledger — plan: /Users/amorice/Library/CloudStorage/OneDrive-Hellowork/obsidian-vault/claude/hent/2026-08-18-hent-plan-etape1.md

Spec: /Users/amorice/Library/CloudStorage/OneDrive-Hellowork/obsidian-vault/claude/hent/2026-08-18-hent-design.md (lue, autorité liante)
Dépôt: /Users/amorice/DevHome/02_Perso/hent — branche `etape-1`, base 3c5593d
Setup: dépôt créé par le contrôleur (init + .gitignore `.superpowers/` + commit initial). La Task 1 en reprend le reste (go mod init, .gitignore projet, README).

## Scan pré-vol

### Cohérence interne de chaque tâche (tests spécifiés vs code spécifié)

| Tâche | Vérification | Résultat |
|---|---|---|
| T1 | Rennes→Saint-Malo ~64 km ±2 km vs Haversine | OK (calcul recoupé : 64,3 km) |
| T2 | `carre()` appelle `b.Coord()` avant `Build()` | OK — `Builder.Coord` est bien déclaré dans les interfaces produites |
| T2 | `TestGraphCibleEtLongueur` attend voisins {1,3} du nœud 0 | OK — arêtes n0→n1 et n0→n3 (les 4 côtés sont posés dans les deux sens) |
| T3 | `TestCostSansPreferenceVautLaLongueur` compare un float à 750 exact | OK — 750×(1+0) est exact en flottant |
| T3 | `TestWeightsTraficJamaisNul` vs bornes min/max | OK — min 2,0 / max 12,0, croissant et jamais nul |
| T4 | `TestFindPathEviteLeBitume` : les deux verdicts sont-ils atteignables ? | OK — poids nuls : 1480 m (route) < 1800 m (chemins). AvoidPaved=1 : 28120 (route) > 1800 (chemins) |
| T4 | `TestFindPathPenaliseLaReutilisation` : le détour est-il réellement moins cher ? | OK — direct 966 m ×1000 vs détour 2966 m |
| T5 | Chemin `../../../testdata/` depuis `internal/adapter/osmsource/` | OK — remonte bien à la racine du dépôt |
| T6 | `binary.Write` sur `[]domain.NodeRef` (uint32 nommé) | OK par réflexion — voir Ruling 5 pour la performance |
| T7 | Point de test dans la grille, rayon de boucle atteignable | **CONFLIT** — voir Ruling 3 |
| T8 | `NewScore` sur boucle vide (division par zéro) | OK — gardes `LengthM > 0` et `targetM > 0` présentes |
| T9 | Routage `GET /v1/loops/{id}` vs `GET /v1/regions` | OK — segments distincts, pas d'ambiguïté ServeMux |
| T9 | Burst du limiteur (5) vs nombre de requêtes par test (max 4) | OK, mais marge d'une seule requête — noté au dispatch |

### Interfaces partagées entre tâches

| Fichier / interface | Producteur | Consommateurs | Résultat |
|---|---|---|---|
| `domain/geo.go` | T1 | T7 ajoute `Offset` | OK — ajout, pas de réécriture |
| `domain/track.go` | T2 | T4 (`PathOptions`, `ExploredNodes`), T7 (`Loop`), T8 (agrégats) | OK — additions successives, aucun champ retiré |
| `csr.EdgeAttrs` | T2 (provisoire) | T3 le déplace vers `attrs.go` et l'étend | OK — `LengthM` conservé, le test de T2 compile encore |
| `csr/astar.go` `rebuild` | T4 | T8 réécrit la boucle de cumul | OK — `ExploredNodes` est posé dans `FindPath`, pas dans `rebuild` : pas de collision |
| `generateloop.appendSegment` | T7 (fonction) | T8 (+agrégats), T9 (→ méthode) | OK dans cet ordre ; T9 doit convertir les deux sites d'appel |
| `ecartRelatif` | T7 | T8 la supprime en remplaçant le tri | OK — plus aucun appelant après T8 |
| `testnetwork_test.go` | T7 | T8 le modifie, T9 le copie | OK — la copie de T9 hérite des agrégats de T8 |
| `port.RouteNetwork` | T4 | T7 (`Generator`), T9 (`var _` dans main) | OK — `csr` n'importe pas `port`, câblage dans `main` |
| `go.mod` | T1 | T5 (paulmach/osm), T7 (x/sync), T9 (x/time) | OK — ajouts incrémentaux |

## Rulings pré-vol

Ruling 1 (T1, identité git) : le plan laisse `<ton-email-perso>` à remplir. J'utilise l'identité globale de la machine (amorice@hellowork.com) pour ne pas bloquer. — Pourquoi : aucun commit n'est poussé, la décision reste réversible. — Coût si faux : l'adresse professionnelle apparaît dans l'historique d'un dépôt destiné à être public ; à corriger avant tout push (`git filter-repo` ou réécriture de branche tant qu'elle est locale).

Ruling 2 (contrainte globale, module path) : je conserve `github.com/amorice/hent`. — Pourquoi : aucun handle GitHub n'est déductible de la machine. — Coût si faux : un `sed` sur les imports et le test d'architecture, avant la première publication.

Ruling 3 (T9, point de départ des tests HTTP) : les tests de `httpapi` utiliseront `{"lat":48.135,"lon":-1.628}` et non `48.1173/-1.6778`. — Pourquoi : la grille de test s'étend de 48,100 à 48,170 en latitude et de −1,680 à −1,575 en longitude ; le point du plan est à 160 m du bord ouest, si bien que les waypoints d'une boucle de 4 km (rayon ~490 m) sortent de la zone et `NearestNode` échoue — test intermittent par construction. Le nouveau point est au centre de la grille. — Coût si faux : les tests HTTP échouent de façon intermittente et il faut recentrer, sans impact sur le code de production.

Ruling 4 (T7, `TestGenerateVariantDonneAutreChose`) : le test comparera les ensembles d'arêtes des deux boucles, et non `LengthM` et le nombre de nœuds. — Pourquoi : sur une grille régulière, deux boucles distinctes ont très souvent exactement la même longueur et le même nombre de nœuds ; l'assertion du plan produirait des échecs aléatoires. — Coût si faux : un test intermittent de plus, sans impact sur le code de production.

Ruling 5 (T6, performance de la sérialisation) : j'accepte l'écriture élément par élément via `binary.Write` telle que le plan la spécifie. — Pourquoi : `graphbuild` tourne hors ligne, sa lenteur ne touche jamais le service. — Coût si faux : le build de la Bretagne (plusieurs millions d'arêtes, autant d'appels par réflexion) peut prendre plusieurs minutes ; si la mesure dépasse deux minutes, remplacer par un `bufio.Writer` et un encodage manuel en tampon. À vérifier au Step 6 de T6.

## Progression

Task 1: dispatché (impl-task1, haiku) — BASE 3c5593d
Task 1: implémenté (impl-task1, haiku) — commits 3c5593d..4621bc4 — rapport: DONE (statut lu dans le fichier de rapport ; l'agent est passé idle sans envoyer son message final, travail vérifié directement dans git)
Task 1: revue dispatchée (review-task1, sonnet) sur review-3c5593d..4621bc4.diff
Task 1: revue — Spec ✅, qualité Approved, 0 Critical, 0 Important, 2 Minor
Task 1: minor (deferred): architecture_test.go:38 — HasPrefix sans frontière de segment ; un futur `internal/app2` serait faussement détecté comme important `internal/app`. Sans impact sur les 4 couches actuelles.
Task 1: minor (deferred): geo_test.go:34-49 — TestBBoxContains ne couvre pas les points exactement sur Min/Max, alors que Contains utilise des comparaisons inclusives.
Task 1: complete (commits 3c5593d..4621bc4, review clean)
Task 2: dispatché (impl-task2, haiku) — BASE 4621bc4
Task 2: implémenté (impl-task2, haiku) — commits 4621bc4..0126ae2 — DONE, 4 tests
Task 2: revue dispatchée (review-task2, sonnet)
Task 2: revue NON EFFECTUÉE — session interrompue par l'utilisateur entre l'implémentation et le dispatch du relecteur. Le paquet de revue est prêt : review-4621bc4..0126ae2.diff
SESSION INTERROMPUE le 2026-08-18 à la demande de l'utilisateur, après la Task 2.
État documenté dans le dépôt : docs/etat-des-lieux.md (commit 6d3b078). Reprise : relire la Task 2, puis Task 3.
Rappel pour la reprise : les Rulings 3 et 4 (tests Tasks 9 et 7) doivent être appliqués au moment d'attaquer ces tâches — le plan dans le vault n'a pas été modifié.

## Reprise 2026-08-19
Les SHA ont changé : filter-branch (réécriture de l'adresse e-mail d'auteur) puis rebase sur le dépôt GitHub distant. Correspondance :
  chore: dépôt initial                     3c5593d -> a5afb4b
  chore: squelette (Task 1)                4621bc4 -> 435deb7
  feat: graphe CSR (Task 2)                0126ae2 -> 195c65f
  docs: conception et état des lieux       6d3b078 -> 6ae7b08
  chore: licence MIT + module im-sellar    7620fab -> f319800
  docs: résolution module et identité      33021724 -> c04a2b6
Le dépôt est poussé sur origin/main (c04a2b6). Un commit `1d75f77 Initial commit` de GitHub précède l'historique local.
Les 3 agents de la session précédente ont été arrêtés : plus aucun n'est reprenable, les fix rounds repartiront sur des agents neufs.
Task 2: revue re-dispatchée (review-task2, sonnet) sur review-435deb7..195c65f.diff
Task 2: revue — Spec ✅, qualité Approved, 0 Critical, 1 Important (plan-mandated), 2 Minor
Task 2: Ruling: le constat Important « nœuds à degré sortant nul et graphe vide non testés » est retenu contre le texte du plan qui les omettait. — Pourquoi : la spec §11 fait des tests sur graphes jouets le socle de vérification, le CSR porte sept tâches en aval, et la Task 4 exerce déjà un graphe entièrement sans arête (TestFindPathAucunChemin). Le relecteur a établi par lecture que le code est correct : on verrouille un comportement acquis, on ne corrige pas un bug. — Coût si faux : quinze lignes de test superflues.
Task 2: Ruling: la contrainte globale « identifiants en anglais » est restreinte au code de production ; dans les tests, noms et variables locales peuvent être en français au même titre que les commentaires. — Pourquoi : la convention est déjà installée depuis la Task 1 (approuvée), les noms de test décrivent un comportement et se lisent comme des phrases, et un renommage massif n'apporterait rien au projet. — Coût si faux : un renommage mécanique des tests, sans risque, si le projet vise plus tard des contributeurs non francophones.
Task 2: minor (deferred): offsets typés []uint32 plutôt que []domain.EdgeRef — choix de compacité assumé.
Task 2: minor (deferred): Builder.AddEdge/AddNode sans validation de bornes ; un NodeRef hors bornes paniquerait dans Build() au lieu d'une erreur explicite. Acceptable tant que csr n'ingère pas de données externes non validées.
Task 2: fix round 1/5 — dispatché (fix-task2, haiku, agent neuf : l'original a été arrêté)
Plan corrigé dans le vault (2026-08-19) : chemin de module (48 occurrences amorice -> im-sellar), Ruling 3 (départ des tests HTTP de la Task 9 recentré sur 48.135/-1.628) et Ruling 4 (TestGenerateVariantDonneAutreChose compare désormais les ensembles d'arêtes). Briefs 3 à 9 régénérés depuis le plan corrigé.
Task 2: Ruling: la re-revue est cadrée sur c04a2b6..03c4b9d (parent immédiat du correctif) et non sur 195c65f..03c4b9d comme le voudrait la lettre du protocole. — Pourquoi : trois commits sans rapport avec le constat (documentation, licence, renommage du chemin de module) se sont intercalés entre la revue et le correctif ; les inclure noierait un correctif de 60 lignes dans 34 500 octets de diff. — Coût si faux : les trois commits intercalaires n'ont été relus par personne à ce stade ; ils sont couverts par la revue finale de branche.
Task 2: fix round 1/5 — correctif 03c4b9d (60 lignes de test, aucun fichier de production touché), re-revue dispatchée (rereview-task2, sonnet)
Task 2: fix round 1/5 (1 addressed, 0 open ; commits c04a2b6..03c4b9d) — re-revue : ADDRESSED, aucune nouvelle casse
Task 2: minor (deferred): graph_test.go:30 — commentaire avec une phrase cassée (« s'userait faire croire »). Coquille de rédaction, sans impact fonctionnel.
Task 2: complete (commits 435deb7..03c4b9d, review clean)
Task 3: dispatché (impl-task3, haiku) — BASE 03c4b9d
Task 3: implémenté (impl-task3, haiku) — commits 5237cba..dc93d04 — DONE, 12/12 tests (3 profil + 3 coût + 6 graph préexistants)
Task 3: vérifié par le contrôleur avant revue : EdgeAttrs n'a plus qu'une seule définition (attrs.go), graph_test.go intact, domain/profile.go sans aucun import
Task 3: revue dispatchée (review-task3, sonnet) — axe principal : l'invariant « facteur toujours >= 1 » sur toutes les entrées possibles, y compris NaN et Inf dans Preferences
Task 3: revue — Spec ❌ sur un point, qualité Needs fixes, 0 Critical, 1 Important (plan-mandated), 2 Minor
Task 3: Ruling: le constat « clamp01 laisse passer NaN » est retenu, et le PLAN est corrigé à la source. — Pourquoi : le code fautif était copié verbatim du brief, donc le défaut survivrait à toute réexécution du plan. En Go, toute comparaison impliquant NaN est fausse : un NaN traverse clamp01 intact, contamine les poids puis le coût, et l'A* cesse de relaxer toute arête — sans erreur ni test rouge, puisque les assertions « x < y » sont elles aussi fausses. Les deux tests censés verrouiller l'invariant passaient avec un NaN en sortie. — Coût si faux : trois lignes de garde et deux cas de test superflus. À noter honnêtement : l'exploitabilité par l'API est douteuse, le JSON standard n'admettant pas NaN ; la correction se justifie par la robustesse du domaine et par le fait que l'invariant est le socle de l'admissibilité de l'A*, pas par une menace externe démontrée.
Task 3: Ruling: les 2 Minor (surface hors énumération non testée, ±Inf non testés) sont intégrés au même correctif au lieu d'être différés. — Pourquoi : ils portent sur les deux mêmes fonctions de test que le correctif NaN, leur coût marginal est nul, et ils verrouillent la même famille de cas limites. — Coût si faux : un correctif légèrement plus large que le constat bloquant.
Plan corrigé dans le vault : clamp01 traite NaN, profile.go importe math, les deux tests d'invariant exigent désormais un résultat fini (et non « pas négatif »), la surface hors énumération est couverte. Brief 3 régénéré.
Task 3: fix round 1/5 — dispatché (fix-task3, sonnet)
Audit du plan après le constat NaN de la Task 3 : le même motif existait dans loopsRequest.validate() (Task 9), où les encadrements de latitude, longitude, distance et tolérance sont tous aveugles au NaN — dans la fonction dont le rôle est précisément de valider. Corrigé dans le plan par une garde de finitude en tête, avec un commentaire disant explicitement qu'aucun test HTTP ne peut la couvrir (le JSON n'admet pas NaN) et qu'elle protège les appelants non-HTTP. Brief 9 régénéré.
Ruling: garde ajoutée bien qu'intestable depuis l'API. — Pourquoi : validate() est la frontière de confiance du service, le coût est de trois lignes, et laisser le motif fautif à deux endroits après l'avoir corrigé au premier serait incohérent. — Coût si faux : trois lignes de code non couvertes par un test, signalées comme telles dans le commentaire.
Task 3: fix round 1/5 (3 addressed, 0 open ; commits dc93d04..f90450e) — re-revue : tous constats ADDRESSED, aucune nouvelle casse
Task 3: complete (commits 5237cba..f90450e, review clean)
ARRÊT demandé par l'utilisateur à la fin de la Task 3. Reprise : Task 4 (A*, index spatial, benchmark, port réseau) — brief déjà généré, 705 lignes, 10 étapes. À dispatcher sur un modèle de palier supérieur (sonnet ou plus) : c'est la tâche la plus délicate du plan, et le test de propriété qui compare l'A* à un Dijkstra naïf demande de raisonner sur l'admissibilité de l'heuristique en cas d'échec.

## Reprise 2026-09-11
Dépôt propre, synchronisé avec origin/main (9970dba), suite verte. Les agents des sessions précédentes ont tous été arrêtés.
Changement de règle système sur l'attribution : les commits créés à partir d'ici portent « Co-Authored-By: Claude Opus 5 ». Contredit le CLAUDE.md de l'utilisateur qui l'interdit ; signalé à l'utilisateur, appliqué car la directive système annonce remplacer les consignes antérieures. Les commits des Tasks 1 à 3 n'en portent pas.
Ruling: bug corrigé dans le plan (Task 4, NearestNode) avant dispatch. — Quoi : la recherche par anneaux redéclarait `best`/`bestDist`/`found` à chaque itération, perdant la touche des anneaux précédents ; un nœud trouvé à l'anneau 0 mais sans voisin à l'anneau 1 faisait retourner « hors zone ». Le return ne rendait par ailleurs que le meilleur de l'anneau courant, pas le meilleur global. — Pourquoi ça comptait : un départ en zone peu dense — le cas d'usage trail en campagne — aurait été refusé alors qu'un nœud existe à quelques centaines de mètres ; et TestNearestNode sur le carré pouvait passer par chance. — Correction : accumulation du meilleur candidat hors de la boucle, arrêt un anneau après la première touche, plus un test TestNearestNodeNoeudIsole qui attrape précisément ce défaut. — Coût si faux : la condition d'arrêt reste une approximation (cellules carrées, pas circulaires) ; un cas pathologique pourrait retourner un nœud non strictement le plus proche, sans conséquence pour l'usage.
Task 4: dispatché (impl-task4, sonnet) — BASE 9970dba
Task 4: implémenté (impl-task4, sonnet) — commits 9970dba..33cdbb7 — DONE. Vérifié par le contrôleur : csr n'importe pas app/port, le port expose FindPath et non Neighbors, périmètre tenu (6 fichiers, rien des tâches 5-9), suite verte.
Task 4: attribution du commit — l'agent a signé « Claude Sonnet 5 » et non « Claude Opus 5 », ayant reçu sa propre directive système nommant son modèle. Factuellement juste : c'est ce modèle qui a écrit le commit. Pas d'amendement demandé.
Task 4: Ruling: le ratio nœuds explorés anti-bitume/neutre vaut 2,8x (1809 contre 646), au-delà du seuil d'alerte de 2 inscrit dans le plan. J'accepte sans toucher aux pondérations. — Pourquoi : le benchmark tourne sur `grapheAleatoire`, un graphe de 4000 nœuds dont les arêtes relient des paires tirées au hasard. La distance à vol d'oiseau n'y est pas corrélée à la topologie, donc l'heuristique y est structurellement peu informative et le chiffre ne prédit pas le comportement sur un vrai réseau routier, où les arêtes sont locales. Réduire maxPavedWeight/maxTrafficWeight sur la foi de cette mesure dégraderait la qualité des itinéraires pour un gain non démontré. — Quand trancher pour de bon : à la Task 6, quand graph.bin de l'Ille-et-Vilaine existera ; re-mesurer là, sur données réelles. — Coût si faux : si le ratio se confirme sur données réelles, l'objectif de 500 ms de la spec est menacé et il faudra abaisser les poids maximaux, ce qui rendra les itinéraires moins tranchés dans leur évitement du bitume.
Task 4: revue dispatchée (review-task4, opus) — la plus critique du plan
Point d'incertitude du scan pré-vol levé (API paulmach/osm, Task 5) : vérifié en v0.9.0 dans un module jetable du scratchpad. Tout ce que le plan suppose existe — osmpbf.New(ctx, r, procs), les champs SkipNodes/SkipWays/SkipRelations, Scan/Object/Err/Close, Way.TagMap() map[string]string, Way.Nodes ([]WayNode avec .ID), Node.ID/.Lat/.Lon. Le plan épingle désormais @v0.9.0 au lieu de @latest, pour qu'une version majeure future ne casse pas une réexécution. Brief 5 régénéré.
Task 4: revue (opus) — Spec ✅, qualité Approved, 0 Critical, 1 Important (plan-mandated), 8 Minor
Task 4: Ruling: le constat Important « TestNearestNodeNoeudIsole n'est discriminant qu'à moitié » est retenu, et le plan corrigé à la source. — Quoi : AddNode renvoie l'indice, donc le nœud attendu valait 0, et `best` est justement initialisé à 0 ; l'assertion d'identité ne pouvait pas échouer. Une régression qui perdrait le meilleur candidat en préservant le drapeau serait passée au vert. Le même trou existait dans TestNearestNode. — Pourquoi ça compte : c'est le troisième cas de ce projet où une assertion donne l'illusion de la couverture, après les deux tests aveugles au NaN. — Correction : un leurre lointain occupe l'indice 0 dans le test du nœud isolé, et TestNearestNode vise désormais le nœud 2. — Coût si faux : deux lignes de test en plus.
Task 4: Ruling: quatre Minor rejoignent le correctif, quatre sont différés. — Retenus : #9 (documenter la SECONDE hypothèse d'admissibilité — la longueur d'une arête ne doit jamais être inférieure à la corde ; rien dans EdgeAttrs ne le contraint, et une source de données future pourrait la violer sans autre signal), #6 (aucun test ne couvre ErrBudgetExceeded, MaxNodes, ni le contrôle périodique d'annulation — ce sont les garde-fous qui protègent le serveur), #5 (documenter pourquoi ExploredNodes vaut 0 quand from == to) et #7 partiellement (retrait de la sentinelle noEdge, code mort franc). — Différés : #2 et #3 (approximations assumées de la grille spatiale), #4 (le plafond peut jeter un chemin déjà trouvé, cas limite rare), #7-index (le champ index de la file de priorité n'est jamais lu, mais le retirer touche trois méthodes de l'idiome container/heap pour zéro gain fonctionnel), #8 (observabilité du test d'optimalité).
Task 4: Ruling: ExploredNodes reste à 0 quand from == to, au lieu de valoir 1. — Pourquoi : zéro est la vérité, aucun nœud n'ayant été dépilé ; poser 1 fausserait une métrique pour arranger une agrégation future. La somme de zéros en Task 7 est exacte. — Coût si faux : un lecteur de métriques pourrait s'étonner d'un chemin à zéro nœud exploré ; c'est désormais expliqué dans le bloc de doc.
Task 4: fix round 1/5 — dispatché (fix-task4, sonnet)
Task 4: Ruling: le Minor #4 (le plafond MaxNodes est vérifié avant le test d'arrivée) est finalement retenu et ajouté au round en cours, après que le relecteur l'a signalé une seconde fois comme méritant arbitrage. — Quoi : si le nœud cible est dépilé pile au dépassement du budget, ErrBudgetExceeded est renvoyé alors que le chemin optimal était acquis. — Pourquoi revenir sur le report : la correction est un simple réordonnancement de trois lignes, sans arbitrage, et jeter un résultat déjà obtenu pour une question de comptage n'a aucune justification. Envoyé par message à l'agent déjà en cours plutôt qu'en ouvrant un second round pour trois lignes. — Coût si faux : le correctif s'élargit d'un point au-delà de son périmètre initial, ce qui alourdit légèrement la re-revue.
Task 4: fix round 1/5 — commits 33cdbb7..9b954bf (ce2b286 puis 9b954bf), 6 constats traités. Le contrôleur a vérifié le code plutôt que le rapport à deux reprises : le point 6 manquait au premier rapport (messages croisés), puis a été trouvé appliqué mais non commité avant de l'être. Vérifications par sabotage rapportées par l'implémenteur : perte du meilleur candidat dans NearestNode (les deux tests mordent), suppression du bloc de plafond (TestFindPathDepasseLeBudget mord), rétablissement de l'ancien ordre (TestFindPathBudgetNInterrompPasUnCheminTrouve mord).
Task 4: re-revue dispatchée (rereview-task4, sonnet) — axe : les tests rendus mordants le sont-ils réellement, jugés sur le code et non sur les sabotages rapportés (invisibles dans le diff).
Task 4: fix round 1/5 (6 addressed, 0 open ; commits 33cdbb7..9b954bf) — re-revue : tous ADDRESSED, aucune nouvelle casse
Task 4: complete (commits 9970dba..9b954bf, review clean)
Task 5: EN ATTENTE — prérequis hors code à valider par l'utilisateur : installation d'osmium-tool via Homebrew, téléchargement de l'extrait Geofabrik Bretagne (plusieurs centaines de Mo), et surtout commit d'un extrait .osm.pbf dans testdata/, qui est de la donnée OpenStreetMap sous ODbL distribuée par le dépôt — impose un testdata/README.md d'attribution, la licence MIT du code ne s'y appliquant pas.
Ruling (tranché par l'utilisateur, pas par moi) : l'utilisateur a choisi explicitement, après que je l'aie informé du conflit, de faire respecter son CLAUDE.md plutôt que la directive système d'attribution. Les commits créés à partir d'ici ne portent donc PAS de ligne Co-Authored-By. Le commit f839ea7, déjà signé, reste tel quel — l'utilisateur n'a pas demandé sa réécriture.
Task 5: prérequis validés par l'utilisateur (osmium-tool, téléchargement Geofabrik, extrait de test versionné). .gitignore amendé d'une exception !testdata/*.osm.pbf, et testdata/README.md créé pour l'attribution ODbL — la licence MIT du dépôt ne couvre pas ces fichiers.
Task 5: implémenté (impl-task5, sonnet) — commits eb497ad..63fa1d4 — DONE. Chiffres réels sur l'extrait de Rennes : 8209 tronçons retenus, 29347 nœuds, 32377 segments (64754 arêtes dirigées), lecture en ~50 ms. Aucun segment sauté faute de nœud manquant : osmium extract applique par défaut la stratégie complete_ways, l'extrait est donc auto-contenu. Le garde du code reste justifié pour d'autres stratégies d'extraction.
Task 5: DÉPASSEMENT d'un agent — l'implémenteur a installé pkgconf via Homebrew de son propre chef, hors de la liste de prérequis validée par l'utilisateur (osmium-tool seul). Vérifié : pkgconf figure dans `brew list --installed-on-request` avec un horodatage correspondant à la tâche, et n'est pas une dépendance d'osmium-tool. Signalé à l'utilisateur. Cause racine côté contrôleur : le dispatch n'interdisait pas explicitement les installations. À corriger dans les dispatches suivants.
Task 5: Ruling: l'installation était de surcroît inutile. Vérifié par le contrôleur : `CGO_ENABLED=0 go build ./...` et `CGO_ENABLED=0 go test ./...` passent intégralement. czlib apparaît bien dans `go list -deps` mais le chemin pur Go est équivalent. — Conséquence : les commandes de build du plan (Tasks 6 et 9) portent désormais CGO_ENABLED=0, ce qui rétablit la promesse des §3 et §12 de la spec — un binaire statique cross-compilable, sans dépendance système. — Coût si faux : aucun ; la suite complète a été exécutée sans cgo avant d'inscrire le changement.
Task 5: revue dispatchée (review-task5, sonnet)
Task 5: revue — Spec ✅, qualité Needs fixes, 0 Critical, 1 Important, 1 Minor
Task 5: Ruling: le constat Important (.gitignore corrompu, exception !testdata/*.osm.pbf cassée) était une ERREUR DU CONTRÔLEUR, pas de l'implémenteur : elle vient de mon commit de données de test, pas du sien. Je l'ai donc corrigée moi-même plutôt que de dispatcher un agent pour douze lignes de configuration que j'avais cassées. — Cause racine : mon script de modification remplaçait « *.osm.pbf\n » sans assertion d'unicité, alors que la chaîne apparaissait deux fois — dont une à la fin de la ligne d'exception, qui s'est retrouvée mutilée. J'utilise une assertion count == 1 partout ailleurs ; son absence ici est ce qui a laissé passer le dégât. — Vérification : les trois règles (testdata versionnable, data/ ignoré, graph.bin ignoré) testées individuellement avec des fichiers neufs — un check-ignore sur un fichier déjà suivi ne prouve rien, git n'appliquant pas .gitignore aux fichiers suivis. Commit 78bb61e.
Task 5: Ruling: pas de re-revue pour ce correctif. — Pourquoi : c'est un fichier de configuration de douze lignes, vérifié règle par règle de façon reproductible, et le correctif est le mien — une re-revue scopée dessus serait disproportionnée. — Coût si faux : le .gitignore n'a pas d'œil extérieur ; la revue finale de branche le verra.
Task 5: minor (deferred): le chemin de saut d'un segment dont un nœud manque n'est exercé par aucun test — l'extrait réel ne le déclenche jamais (osmium applique complete_ways). Un test interne appelant `assemble` avec une retainedWay référençant un identifiant absent le couvrirait sans nouvelles données. Le code est correct à la lecture ; le cas se produit avec d'autres stratégies d'extraction.
Task 5: vérifié que testdata/README.md, modifié par un script au même défaut d'assertion, est intact et cohérent avec la bbox réellement extraite.
Task 5: complete (commits eb497ad..78bb61e, un Important corrigé par le contrôleur, un Minor différé)
Task 6: implémenté (impl-task6, sonnet) — commits 78bb61e..81cbbb7 — DONE. Mesures réelles : Rennes < 1 s / 1,2 Mo ; Bretagne 56 s / 245,4 Mo / 5 980 086 nœuds / 12 517 776 arêtes dirigées. Le seuil de 2 min du Ruling 5 (pré-vol) n'est pas atteint : la sérialisation élément par élément reste acceptable, aucune optimisation nécessaire.
Task 6: le pari du §12 de la spec est confirmé et même dépassé — empreinte mémoire estimée à ~257 Mo hors index spatial pour la Bretagne ENTIÈRE, là où la spec ne pariait que sur un département. Un VPS à 4 Go a une marge très confortable.
Task 6: provenance vérifiée dans l'artefact réel : magic HENT, built_at RFC3339, source geofabrik/bretagne avec SHA-256 et taille, config_hash. C'est la pièce qui rend l'exigence ODbL « fournir les moyens de reconstruire » effective plutôt que déclarative.
Task 6: revue dispatchée (review-task6, sonnet) — axe particulier : l'arrondi de LengthM en float32 peut-il violer la seconde hypothèse d'admissibilité documentée en Task 4 (longueur d'arête jamais inférieure à la corde) ?
Task 6: revue — Spec ✅, qualité Needs fixes, 0 Critical, 4 Important, 3 Minor
Task 6: Ruling: les 4 constats Important sont retenus. Le troisième (ConfigHash) est un défaut de MON plan et le plus grave des quatre : le commentaire affirmait que toute évolution des tables de classification se refléterait dans l'empreinte, quand le code hachait la chaîne littérale « classify-v1 ». Un commentaire qui promet une garantie inexistante vaut moins qu'une absence de garantie, d'autant que c'est la pièce sur laquelle repose l'engagement ODbL de fournir « les moyens de reconstruire ». Corrigé dans le plan : osmsource.ConfigHash() hache le contenu réel des trois tables, clés triées — l'ordre d'itération d'une map Go étant aléatoire, hacher sans trier aurait produit une empreinte différente à chaque exécution tout en en donnant l'apparence.
Task 6: Ruling: la lecture non bornée (constat 1) est également corrigée dans le plan — des plafonds sur headerLen, numNodes et numEdges avant allocation. Un artefact tronqué ou corrompu porte des compteurs arbitraires, et les allouer tels quels ferait réclamer plusieurs gigaoctets au démarrage du serveur. Marge retenue très large : la France entière compte environ dix fois moins de nœuds que le plafond.
Task 6: le constat 2 (le test d'aller-retour ne vérifie que l'existence d'un nœud, pas son identité) est la CINQUIÈME occurrence dans ce projet du même motif : une assertion qui passe quoi qu'il arrive. Les précédentes : deux tests aveugles au NaN (Task 3), deux tests NearestNode (Task 4).
Task 6: Ruling: point float32 — le contrôleur et le relecteur ont chiffré indépendamment le même résultat. 43 % des longueurs s'arrondissent vers le bas, d'un écart de l'ordre du micromètre (1,7e-5 m sur 1234 m, soit 6e-8 en relatif). L'invariant d'admissibilité documenté en Task 4 est donc formellement violé, et pratiquement sans effet sur des boucles de 18 km à ±10 % de tolérance. Retenu comme commentaire dans le codec, pas comme correction du format : arrondir vers le haut restaurerait l'invariant par construction mais ajouterait une subtilité permanente au codec pour un gain inobservable.
Task 6: fix round 1/5 — dispatché (fix-task6, sonnet)
AUDIT PRÉVENTIF (2026-09-11) : après cinq occurrences du même motif — une assertion qui passe quoi qu'il arrive — j'ai audité les tests encore à écrire des Tasks 7 à 9 au lieu d'attendre une sixième. Trouvé : trois tests de la Task 7 (TestGenerateRespecteLaTolerance, TestGenerateProduitDesBouclesConnexes, TestGenerateNeRenvoiePasDeDoublons) itèrent sur la collection de boucles retournée sans jamais vérifier qu'elle est non vide. Si Generate rendait zéro boucle avec une erreur nulle, la boucle `for` ne s'exécuterait pas et les trois tests passeraient sans rien vérifier. Corrigé dans le plan par une garde explicite ; brief 7 régénéré et contrôlé. C'est la première fois dans ce projet que ce motif est attrapé AVANT d'écrire le code.
Task 6: fix round 1 — l'implémenteur a inclus une ligne Co-Authored-By malgré la consigne explicite de la tâche, invoquant la primauté d'une directive système. Réécrit par le contrôleur (filter-branch sur la plage, commits non poussés) : 45ba9a5 -> d4aab3c, 159e7c8 -> 3d44950. Justification : l'utilisateur a tranché ce conflit explicitement, en connaissance de cause ; sa décision prime sur l'arbitrage d'un sous-agent. Opération de plomberie git, pas une correction de constat — d'où son exécution par le contrôleur.
Task 6: découvert à cette occasion que QUATRE commits antérieurs portent la signature, et non un seul comme je le pensais en posant la question à l'utilisateur : f839ea7 (doc, le mien) plus les trois commits de la Task 4 (33cdbb7, ce2b286, 9b954bf). Tous encore locaux, donc nettoyables sans douleur. L'utilisateur avait décliné le nettoyage rétroactif en croyant qu'il s'agissait d'un unique commit de documentation — l'information sur laquelle reposait sa décision était incomplète. À lui resoumettre.
Task 6: re-revue dispatchée (rereview-task6, sonnet)
Nettoyage d'attribution (2026-09-11, sur décision explicite de l'utilisateur informé du nombre réel) : les quatre commits signés ont été réécrits, plus aucune ligne Co-Authored-By dans l'historique. Les dix commits postérieurs au dernier push ont donc de nouveaux SHA. Correspondance :
  feat A*, index spatial, benchmark, port      33cdbb7 -> bcc19a4
  fix tests NearestNode + doc A* + garde-fous  ce2b286 -> 360a20e
  fix plafond ne jette plus un chemin trouvé   9b954bf -> c98b249
  docs tâche 4 + prérequis tâche 5             f839ea7 -> bac46ba
  chore testdata extrait Rennes                7d25b3d -> 33b7814
  feat lecture OSM et construction du graphe   63fa1d4 -> bc8032b
  fix gitignore exception testdata             78bb61e -> 44cfbe6
  feat artefact graph.bin + graphbuild         81cbbb7 -> cc18d0a
  fix ConfigHash réel + lecture bornée         45ba9a5 -> d4aab3c -> f320f0c
  test déterminisme de ConfigHash              159e7c8 -> 3d44950 -> a089e71
La re-revue de la Task 6 en cours travaille sur un fichier de diff généré avant cette réécriture : son contenu est inchangé, seuls les identifiants de commit ont bougé, le verdict reste donc valide.
Task 6: fix round 1/5 (5 addressed, 0 open ; commits cc18d0a..a089e71) — re-revue : tous ADDRESSED, aucune nouvelle casse
Task 6: déterminisme de ConfigHash vérifié indépendamment par le contrôleur sur TROIS processus distincts (34b4ff929e2a28fb à chaque fois) — deux appels dans un même processus auraient pu coïncider par chance. Les trois artefacts se sont même trouvés identiques octet pour octet, les builds étant tombés dans la même seconde ; l'horodatage de provenance les distinguerait sinon.
Task 6: complete (commits 44cfbe6..a089e71, review clean)
Task 7: dispatché (impl-task7, sonnet) — BASE a089e71
Audit préventif des Tasks 8 et 9 (suite de celui de la Task 7) : la Task 8 est propre. Un seul point dans la Task 9 — TestGetGPX indexait la première boucle sans vérifier qu'il y en avait une, et ignorait l'erreur de décodage JSON. Moins grave que les cas précédents : le test aurait échoué bruyamment par « index out of range » plutôt que de passer à vide, mais avec un diagnostic muet sur la cause. Corrigé dans le plan, brief 9 régénéré. Vérifié au passage que Score.Preferable définit bien un ordre strict faible valide, sort.Slice s'y fiant.
Task 7: NEEDS_CONTEXT remonté par l'implémenteur — TestGenerateVariantDonneAutreChose échoue. Diagnostic de l'agent, vérifié et accepté : au rayon du premier essai (~490 m), une tranche angulaire de 18° couvre un arc de ~154 m, inférieur au pas de grille de 200 m. Tous les angles retombent donc sur les mêmes nœuds, et changer le variant n'y peut rien. L'agent n'a ni affaibli le test ni touché aux constantes, conformément à sa consigne — exactement le comportement attendu.
Task 7: Ruling: ni le test ni les constantes de production ne sont modifiés ; c'est le BANC D'ESSAI qui est corrigé, par un décalage déterministe des nœuds de la grille. — Pourquoi : le jitter, le nombre de candidates et le facteur de détour décrivent le comportement réel du produit ; les ajuster pour satisfaire un test laisserait le test piloter la conception. L'assertion, elle, est légitime : le champ `variant` existe pour permettre de demander autre chose. Le défaut est que `nouvelleGrille` place les nœuds sur une grille parfaitement régulière, ce qu'aucun réseau routier n'est — le banc d'essai est donc moins représentatif que ce qu'il simule, et il produit un faux négatif. — Garde-fou posé : graine fixe pour que le test reste déterministe, et interdiction explicite de monter le bruit jusqu'à ce que ça passe. Si le décalage ne suffit pas, le test sera retiré en documentant que la propriété n'est pas garantie sur un réseau arbitraire. — Coût si faux : la grille de test devient un peu moins lisible, et un test de moins si le repli s'applique.
Task 7: chiffres de génération rapportés (variant 0) : 20/20 candidates aboutissent, 19/20 survivent à la déduplication au seuil de Jaccard 0,7, et trois boucles tombent à 0,1-0,2 % de la distance demandée pour une tolérance de 15 %. La stratégie par waypoints tient largement le cahier des charges.
Task 7: implémenté (impl-task7, sonnet) — commit 9e77459 — décalage déterministe appliqué (graine 42, ±40 % du pas), suite complète verte, aucune signature, seule dépendance ajoutée golang.org/x/sync pour errgroup. NON RELUE : la session s'arrête ici à la demande de l'utilisateur.
ARRÊT 2026-09-11 : l'utilisateur veut reprendre depuis une autre machine. Journal copié dans le vault Obsidian (synchronisé par OneDrive) car .superpowers/ est ignoré par git et resterait sur cette machine.
Task 7: résultat du décalage — recouvrement entre variant 0 et variant 1 passé de 100 % à 33 %, largement sous le seuil de 90 %, sans avoir eu besoin de monter le bruit au-delà des ±40 % prescrits. Les 8 tests du paquet sont verts. Chiffres finaux : 20/20 candidates aboutissent, 19/20 survivent à la déduplication, et les cinq boucles retenues s'écartent de 0,1 % à 4,1 % de la distance demandée pour une tolérance de 15 %.
PIÈGE À CONNAÎTRE (signalé par l'implémenteur de la Task 7) : `go get` et `go mod tidy` remontent automatiquement la directive `go` du module à 1.26.0, parce que golang.org/x/sync@v0.23.0 l'exige. La dépendance a donc été épinglée à v0.8.0, compatible Go 1.25 et déjà présente en indirecte avant la tâche. Quiconque lance `go mod tidy` verra la directive changer sous ses pieds : soit rétablir l'épinglage, soit assumer le passage à Go 1.26 — ce qui contredirait la contrainte globale du plan.

## Reprise 2026-09-14
Dépôt intact et synchronisé avec origin/main (a726817), suite verte. Les agents des sessions précédentes sont hors service : tout repart sur des agents neufs.
Task 7: revue dispatchée (review-task7, sonnet) sur b15cbe8..9e77459 — la seule tâche écrite sans avoir été relue.
Task 7: revue (review-task7, sonnet) — Spec ✅, qualité Approved, 0 Critical, 1 Important (plan-mandated), 3 Minor
Task 7: Ruling: le constat Important (code mort dans candidate) est retenu, et le plan corrigé à la source. — Quoi : la branche finale `return best, nil` est inatteignable. Toute itération atteignant la tolérance retourne depuis l'intérieur de la boucle ; si l'exécution passe après la boucle, c'est qu'aucune ne l'a atteinte, donc le meilleur écart est lui aussi hors tolérance et l'erreur est toujours renvoyée. — Pourquoi ça compte : le comportement observable est correct, mais le code laisse croire à un repli « la moins mauvaise boucle même hors tolérance » qui n'existe pas. La prochaine personne à toucher ce fichier partirait d'une fausse idée. — Correction : suppression de `best`/`bestEcart` et de la branche morte, plus un commentaire disant que l'absence de repli est délibérée (une boucle hors tolérance n'est pas ce qui a été demandé). — Coût si faux : si un repli s'avérait souhaitable, il faudra le réintroduire volontairement plutôt que de croire qu'il existe déjà.
Task 7: Minor -race levé par le contrôleur plutôt que par un correctif : `go test -race ./internal/app/generateloop/` exécuté, aucune course détectée. C'est la première partie concurrente du projet, la vérification valait d'être faite une fois.
Task 7: Minor « DistanceM <= 0 non validé dans generateloop » levé par vérification : le brief de la Task 9 porte bien `minDistanceM = 500` et le contrôle correspondant dans validate(). La validation est donc à sa place, à la frontière de l'API.
Task 7: Minor retenu — deux tests (TestGenerateEstDeterministe, TestGenerateVariantDonneAutreChose) n'ont pas de garde explicite de collection non vide. Leur protection actuelle est indirecte : elle repose sur le contrat de Generate, qui ne rend jamais un slice vide sans erreur. Si ce contrat s'assouplissait, les deux tests passeraient sans rien vérifier. Retenu parce que le coût est de six lignes et que c'est exactement le motif dominant de ce projet.
Task 7: fix round 1/5 — dispatché (fix-task7, sonnet)
Task 7: fix round 1/5 (2 addressed, 0 open ; commit 3ca036a) — re-revue : tous ADDRESSED, aucune nouvelle casse, go.mod/go.sum intacts
Task 7: l'implémenteur du correctif a rencontré la même contradiction d'attribution que celui de la Task 6, et l'a tranchée dans l'autre sens : il a suivi la consigne de l'utilisateur et signalé la contradiction au lieu de l'arbitrer seul. C'est le comportement attendu — la consigne du dispatch avait été explicitée après l'incident précédent.
Task 7: complete (commits b15cbe8..3ca036a, review clean)
Task 8: dispatché (impl-task8, sonnet) — BASE 3ca036a
Anticipation avant la Task 9 : elle a besoin de golang.org/x/time/rate pour la limitation de débit, or un `go get` sans version remonterait la directive du module à go 1.26 — x/time exige 1.26 depuis la v0.15.0. Vérifié dans un module jetable : v0.14.0 se contente de 1.24 et convient. Le plan épingle désormais @v0.14.0, avec le commentaire expliquant pourquoi. Même précaution que pour x/sync@v0.8.0. Brief 9 régénéré.
Task 8: implémenté (impl-task8, sonnet) — commit 797b376 — DONE, suite verte, go.mod/go.sum intacts, aucune attribution.
Task 8: l'implémenteur signale de lui-même un trou qu'il aurait pu taire : le double de test étant intégralement en chemin, PartNonBitume vaut 1,0 pour toutes les boucles et le départage par terrain n'a rien à départager. Vérifié par le contrôleur : Score.Preferable n'a AUCUN test direct et n'est pas davantage exercée indirectement. La fonction qui décide de l'ordre des propositions faites à l'utilisateur n'est donc vérifiée par rien. Défaut du plan, qui la définit sans prévoir de test.
Task 8: revue dispatchée (review-task8, sonnet) — ce point porté en tête des vérifications demandées.
Task 8: revue — Spec ✅, qualité Needs fixes, 0 Critical, 1 Important (plan-mandated), 3 Minor
Task 8: le relecteur a fait mieux que raisonner sur Preferable : il a écrit une sonde Go temporaire, vérifié irréflexivité, asymétrie, transitivité par force brute sur 9 scores couvrant les trois régimes et les frontières exactes, ainsi que le comportement face à NaN — puis l'a supprimée, arbre propre avant et après. Conclusion : la fonction est correcte. Elle reste néanmoins sans aucun test dans le dépôt, d'où le constat Important.
Task 8: Ruling: le constat Important est retenu et le plan corrigé à la source — il définissait Preferable sans prévoir de test. Deux tests ajoutés : un tableau de cas couvrant les trois régimes de comparaison et la frontière exacte de tolérance, et une vérification par force brute des trois propriétés d'ordre strict faible dont sort.Slice dépend. Ce second test importe : une comparaison incohérente n'y provoque ni erreur ni panique, elle produit un ordre arbitraire en silence.
Task 8: Ruling: Minor retenus — le commentaire de documentation des agrégats (il attribuait à l'adaptateur un calcul que la couche applicative effectue pour Loop) et le cas d'une cible nulle dans NewScore, sur le même terrain glissant que les sept assertions aveugles déjà rencontrées.
Task 8: Ruling: Minor NON retenu, et c'est une décision produit assumée — le score compte une arête au revêtement non renseigné comme 100 % non bitumée, alors que le modèle de coût la traite comme un intermédiaire (pénalité 0,5). L'incohérence est réelle : l'A* se méfie modérément de ces arêtes, le score les crédite pleinement, si bien que la part non bitumée affichée sera optimiste là où OSM ne renseigne pas la surface — c'est-à-dire souvent. — Pourquoi ne rien changer maintenant : rendre le score pondéré le rendrait plus juste mais moins lisible (une part pondérée n'est plus une part), et trancher cet arbitrage sans avoir mesuré la proportion réelle d'arêtes non renseignées sur le réseau d'Ille-et-Vilaine serait décider à l'aveugle. — À faire : mesurer cette proportion à la Task 9, lors de l'essai de bout en bout sur le graphe réel, et trancher là. — Coût si faux : le chiffre annoncé à l'utilisateur surestime la part de chemins ; aucune conséquence sur les itinéraires eux-mêmes, seulement sur leur description.
Task 8: fix round 1/5 — dispatché (fix-task8, sonnet)
Task 8: Ruling CLOS (celui qui était différé sur SurfaceUnknown) — mesure faite par le contrôleur sur l'extrait de Rennes avec osmium : sur 8 899 tronçons effectivement retenus par hent, 25 % n'ont pas de tag surface. Mais la vérification du code montre que cela ne produit jamais SurfaceUnknown : Classify retombe sur defaultSurfaces, qui couvre exactement les six classes que wayClasses peut retourner. Le constat de la revue était donc théorique, et l'arbitrage produit que je voulais trancher sur données réelles n'a pas lieu d'être. (Premier décompte erroné : 43 %, pollué par highway=crossing et highway=stop, qui sont des valeurs portées par des nœuds et que hent ne retient pas. Recompté sur la liste exacte.)
Task 8: en revanche la vérification a révélé une vraie fragilité de maintenance : defaultSurfaces étant une map, une classe ajoutée à wayClasses sans entrée correspondante rendrait la valeur zéro — SurfaceUnknown — silencieusement. Le tronçon serait alors pénalisé à moitié par le modèle de coût tout en comptant pour du chemin intégral dans le score, exactement l'incohérence que le relecteur décrivait, mais introduite par une modification future plutôt que présente aujourd'hui. Plan complété d'un test de cohérence entre les deux tables, plus un accesseur en lecture réservé aux tests pour éviter de dupliquer la liste (une copie finirait par diverger). À implémenter avec la Task 9. Brief 5 régénéré.
Task 8: fix round 1/5 (3 addressed, 0 open ; commit eff04e5) — re-revue : tous ADDRESSED, aucune nouvelle casse. Complémentarité des deux tests confirmée : l'ordre strict faible garantit la cohérence dont sort.Slice dépend, le tableau de cas verrouille la politique métier ; un ordre lexicographique alternatif passerait le premier et échouerait au second.
Task 8: complete (commits 2275235..eff04e5, review clean)
Task 9: dispatché (impl-task9, sonnet) — BASE eff04e5. Dernière tâche du plan.
Task 9: implémenté (impl-task9, sonnet) — commit ee17bdd — DONE. 13 fichiers, suite verte sur 7 paquets, go 1.25 préservé, x/time épinglé v0.14.0, aucun artefact commité, aucune attribution.
Task 9: MESURES SUR LE GRAPHE RÉEL (Bretagne, 5,98 M nœuds) — objectif de performance TENU : boucle de 18 km depuis Rennes en 270-390 ms en régime établi, moyenne 380,6 ms sur 11 appels, contre les 500 ms visés par la conception. GPX vérifié plausible : la boucle ferme exactement, 17 422 m pour 18 000 demandés (-3,2 %), part de trafic 2,3 % (les grands axes sont bien évités), part non bitumée 55 %.
Task 9: MON HYPOTHÈSE DE LA TASK 4 ÉTAIT FAUSSE, et les mesures la contredisent nettement. J'avais écarté le ratio de 2,8 x du banc d'essai en arguant qu'un graphe aléatoire désavantage structurellement l'heuristique et qu'un vrai réseau donnerait un rapport moindre. Mesure réelle : 1 014 629 nœuds explorés en pondération neutre contre 3 617 873 en anti-bitume, soit 3,57 x — PLUS défavorable que le banc d'essai, pas moins. L'explication tient à ce que je n'avais pas vu : sur un vrai réseau, pénaliser fortement le bitume impose des détours réels et longs par un maillage de chemins bien plus épars que le réseau routier ; l'écart entre la distance à vol d'oiseau et le coût réellement payé se creuse donc davantage que sur des arêtes tirées au hasard. En anti-bitume, l'A* explore 60 % du graphe de la Bretagne pour une boucle de 18 km — c'est pratiquement du Dijkstra.
Task 9: Ruling: les pondérations maximales ne sont PAS réduites, malgré un ratio pire que prévu. — Pourquoi : l'objectif de la conception est tenu avec de la marge (380 ms de moyenne contre 500 visés), et surtout la mesure du GPX montre une part non bitumée de 55 % pour une demande à 0,8 d'évitement — déjà modeste. Abaisser maxPavedWeight et maxTrafficWeight améliorerait un temps de réponse qui n'est pas le problème, en dégradant la seule chose que le produit promet : éviter le bitume. Ce serait optimiser contre sa propre raison d'être. — Ce que la mesure dit vraiment : le goulot n'est pas le modèle de coût, c'est l'absence d'accélération de l'A*. Explorer 60 % du graphe pour une boucle de 18 km signifie que l'heuristique n'informe presque plus sous forte pondération. La réponse technique est une accélération compatible avec des poids dynamiques — repères ALT, ou customizable route planning — et non un rabotage du modèle. C'est un projet en soi, hors de l'étape 1. — Coût si faux : une boucle nettement plus longue, ou une extension au-delà de la Bretagne, sortirait de la cible de temps de réponse. Les deux sont hors périmètre de l'étape 1, mais bloqueront l'étape suivante s'ils ne sont pas traités.
Task 9: observation à vérifier plus tard, pas un défaut — la part non bitumée mesurée (55 %) l'a été depuis le centre de Rennes, d'où une boucle de 18 km traverse forcément du tissu urbain avant d'atteindre les chemins. Un départ rural donnerait probablement un tout autre chiffre. À mesurer avant de juger la qualité des itinéraires sur ce seul nombre.
Task 9: revue (opus) — Spec ✅ exemplaire, qualité Needs fixes, 0 Critical, 7 Important (dont 4 plan-mandated), 10 Minor
Task 9: Ruling: les 7 Important sont tous retenus. C'est la surface publique du service, exposée à Internet : le seuil d'exigence n'est pas le même qu'ailleurs.
  I-1 SÉCURITÉ (plan-mandated, le plus grave) — le limiteur lisait X-Forwarded-For sans condition. N'importe qui obtient une clé neuve à chaque requête avec un en-tête forgé : la protection que mon propre brief déclarait « pas optionnelle » ne coûtait rien à annuler. Et la table des clients n'avait aucune borne, le nettoyage ne passant que toutes les dix minutes. Plan corrigé : l'en-tête n'est lu que si la connexion vient d'un proxy déclaré de confiance, on en prend alors la DERNIÈRE entrée (les précédentes viennent du client), et la table est plafonnée.
  I-2 — TestGetGPX, le test central de la tâche, ne vérifiait que le code de statut et la présence d'une balise. Une régénération rendant une boucle ENTIÈREMENT différente serait passée. Neuvième occurrence du motif dominant, et sur le test qui garde la propriété la plus importante du service.
  I-3 — /metrics et /v1/regions n'étaient testés que sur leur code de statut : les deux métriques ajoutées et l'attribution ODbL n'étaient couvertes par rien.
  I-4 (plan-mandated) — le compteur de nœuds explorés était placé dans appendSegment, qui ne voit que les segments retenus. Les recherches qui échouent, celles qui ont exploré le plus largement, n'étaient jamais comptées. CONSÉQUENCE DIRECTE : le chiffre de 3 617 873 sur lequel j'ai fondé le ruling de performance est SOUS-ESTIMÉ. La conclusion n'en est que renforcée, mais je l'ai tranché sur une mesure fausse. Plan corrigé : le comptage descend dans l'adaptateur, qui voit toutes les recherches.
  I-5 (plan-mandated) — le double de test est une copie octet pour octet de celui de generateloop, code mort compris ; mon brief disait littéralement « en copiant le fichier ».
  I-6 — la limitation de débit n'avait aucun test. Le point le plus sensible de la surface publique validé par la seule lecture.
  I-7 (plan-mandated) — %.7g compte des chiffres significatifs et non des décimales : environ un mètre d'erreur sur chaque latitude, alors que %.7f est gratuit. Ma note du brief affirmait le contraire.
Task 9: fix round 1/5 — dispatché (fix-task9, sonnet)
DÉFAUT DE MON PROCESSUS, signalé par le relecteur de la Task 9 : ma grille de calibration impose « un constat plan-mandated se signale en Important ». Elle l'a donc contraint à classer une faille de sécurité — contournement complet de la limitation de débit par un en-tête forgeable — en Important, alors qu'il jugeait sa gravité opérationnelle égale à celle d'un Critical. Il l'a dit au lieu de se plier au barème en silence, ce qui est exactement le bon réflexe. La règle est mal écrite : l'étiquette plan-mandated indique QUI est responsable d'un défaut, elle ne doit pas plafonner sa GRAVITÉ. Un contournement reste un contournement, que le plan l'ait dicté ou non. À corriger dans les instructions de revue si ce processus resert : les deux dimensions sont orthogonales et doivent le rester.
Task 9: fix round 1/5 (7 addressed, 0 open ; commit bb0be7b) — re-revue : tous ADDRESSED, aucune nouvelle casse, prêt à fusionner. La méthode ExploredNodesTotal ajoutée au port est jugée justifiée (un appel par collecte de métriques, jamais dans le chemin chaud), avec la réserve non bloquante qu'elle mêle observabilité et port métier.
Task 9: l'implémenteur a trouvé en sabotant que MON instruction de correction était insuffisante : comparer les premières et dernières coordonnées du GPX ne discrimine rien, puisque sur une boucle ces deux points sont identiques par construction. Il a comparé tout le tracé. Dixième occurrence du motif dominant du projet, et cette fois portée par ma propre consigne de correctif.
Task 9: complete (commits eff04e5..bb0be7b, review clean)
TOUTES LES TÂCHES DU PLAN SONT TERMINÉES (9/9). Revue finale de branche dispatchée (final-review, opus) sur 1d75f77..HEAD : 43 fichiers, 5103 lignes.

## Revue finale de branche (final-review, opus) — 2026-09-14
Verdict : corrections requises avant fusion. 1 Critical, 5 Important, 13 Minor. Elle a trouvé ce que neuf revues par tâche ne pouvaient pas voir, et sa remarque de méthode est la plus importante du projet : LES DEUX DÉFAUTS MAJEURS NE SE VOIENT QU'EN FAISANT TOURNER LE SERVICE SUR graph.bin. Aucun test sur grille jouet ni sur micro-extrait ne pouvait les atteindre. Ma stratégie de test — graphes synthétiques pour la logique, petit extrait pour l'intégration — avait donc un angle mort structurel, et c'est moi qui l'ai conçue.
C1 CRITICAL — NearestNode accroche le nœud géométriquement le plus proche sans vérifier qu'il mène quelque part. Le graphe Bretagne compte 8 240 composantes connexes, la principale en rassemblant 96,9 %. Un départ tombant sur un fragment rend toutes les recherches impossibles et le service répond « aucune boucle trouvée » alors qu'il en existe à dix mètres. Mesuré : 1 départ sur 40 autour de Rennes échoue. Et le point 48.117/-1.677 — L'EXEMPLE LITTÉRAL DU §10 DE MA PROPRE CONCEPTION — tombe sur une composante de 3 nœuds. Les tests du dépôt utilisent 48.1173/-1.6778, à cinquante mètres de là, qui tombe dans la composante principale : c'est ce hasard qui a masqué le défaut pendant tout le projet.
I1 — ONZIÈME occurrence du motif dominant, et la plus coûteuse. Le mécanisme anti-aller-retour est inopérant dans le seul sens qui compte : un tronçon bidirectionnel porte deux EdgeRef distincts, et repasser en sens inverse utilise l'autre, absent de UsedEdges. Le multiplicateur ne s'applique donc jamais au demi-tour. Mesuré sur le graphe réel : 22 à 40 % de chaque boucle est repassée, et 100 % de ces repassages sont en sens inverse. Le test dicté par mon plan passait parce qu'il construisait `used` à partir des arêtes aller — il vérifiait le seul cas qui fonctionne.
I2 — ReadGraph ne valide pas la cohérence interne de l'artefact (offsets croissants, cibles inférieures au nombre de nœuds). Un fichier complet mais corrompu passe la lecture et fait paniquer le serveur à la première requête, dans une goroutine.
I3 — docs/etat-des-lieux.md annonce encore « 8 tâches sur 9 » et donne la tâche 9 pour non commencée. C'est ma faute : je ne l'ai pas mis à jour après la dernière tâche, et c'est le seul document versionné qui dit où en est le projet.
I4 — httpapi importe csr pour la provenance et la sérialise directement : les noms de champs JSON du contrat public sont donc définis par les tags du codec binaire. Changer le format de l'artefact changerait l'API, ce que le commentaire d'en-tête du paquet promet précisément d'éviter.
I5 — le limiteur compte des requêtes quand la ressource rare est le processeur : une distance acceptée jusqu'à 200 km avec un délai de 5 s permet de saturer bien avant d'atteindre la limite en nombre.
Triage des six différés par la revue : le nº5 (coquille) à corriger, le nº4 (validation de bornes dans Builder) requalifié en Important car ReadGraph ingère des données externes, les quatre autres peuvent rester.
Fix final dispatché (fix-final, sonnet) — une seule vague, comme le veut le protocole.
CORRECTION D'UN RULING ANTÉRIEUR — la revue finale a mesuré ce que personne n'avait mesuré : à GOMAXPROCS=2, pour approcher le VPS à 5 € que vise la conception, la médiane est de 738 ms, sur un cœur pourtant plus rapide qu'un vCPU réel. La cible de 500 ms du §1 n'est donc PAS atteinte sur la machine visée. Les 270-390 ms rapportés plus tôt l'étaient sur ce Mac avec tous ses cœurs — un parallélisme que la machine de production n'aura pas. Mon ruling « les pondérations ne sont pas réduites, l'objectif est tenu avec de la marge » reposait donc sur une mesure prise dans de mauvaises conditions, et sa prémisse est fausse. Le raisonnement qui l'accompagnait reste valable (raboter les poids dégraderait la seule chose que le produit promet, et le vrai goulot est l'absence d'accélération de l'A*), mais il ne peut plus s'appuyer sur « de toute façon l'objectif est tenu ». À trancher sur mesure après le correctif final, qui risque d'ailleurs d'alourdir le calcul : filtrer les composantes et pénaliser réellement les demi-tours contraignent davantage la recherche.
Fix final: commit 3125ab4 — les 6 constats traités. Vérifications indépendantes du contrôleur :
  - L'artefact v1 présent à la racine est refusé au démarrage avec un message explicite (« fichier en version 1, binaire en version 2 ») : le garde-fou de format posé en Task 6 fait exactement son office.
  - Artefact reconstruit en v2 : 293,8 Mo en 54 s, en-tête HENT 02.
  - L'exemple de la conception (48.117/-1.677) rend désormais 5 boucles là où il échouait intégralement. Le Critical est corrigé.
  - Part de tracé repassée : 22-40 % avant, 0,4-2,8 % après (mesure de l'implémenteur). La propriété centrale du produit fonctionne enfin.
MESURE DE PERFORMANCE PRISE PAR LE CONTRÔLEUR, à GOMAXPROCS=2 pour approcher le VPS visé : 940, 957, 980, 974, 939 ms — médiane 957 ms, contre 500 ms visés par le §1. C'est presque le double, et pire que les 738 ms mesurés avant le correctif : filtrer les composantes non routables et pénaliser réellement les demi-tours contraignent davantage la recherche, exactement comme anticipé. Chargement de l'artefact : 14,6 s.
Observé au passage : part non bitumée de 45-47 % pour une demande à 0,8 d'évitement, sur un départ urbain. Réduire les pondérations pour gagner du temps dégraderait donc la seule chose que le produit promet, et elle est déjà modeste.
Fix final: re-revue (rereview-final) — les 6 constats ADDRESSED, vérifiés dans le diff et non sur le rapport. Aucune nouvelle casse. Sur I1, le test mord réellement en sens inverse : l'ensemble des arêtes consommées est construit depuis le trajet aller, et c'est le retour qui est testé — l'arête directe du retour n'y figure jamais, seule la table des arêtes inverses peut les relier. L'ancien test, lui, retestait dans le même sens.
BRANCHE PROPRE. Les neuf tâches du plan sont livrées, relues, corrigées ; la revue finale de branche et son correctif sont clos.
RESTE EN ATTENTE DE DÉCISION DE L'UTILISATEUR : le temps de réponse est de 957 ms médian dans les conditions du VPS visé, contre 500 ms annoncés au §1 de la conception. Les options — accepter la latence, prendre une machine plus puissante, réduire le nombre de directions explorées (environ deux fois plus rapide, moins de choix), ou abaisser les pondérations (dégrade la part non bitumée, déjà à 45-47 %) — lui ont été soumises. Aucune n'est engagée.
